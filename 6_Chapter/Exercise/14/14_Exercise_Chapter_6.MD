### Exercise 6.14
Find the error in the following program fragment and fix it.

```
if (n % 2 == 0);
printf("n is even\n");
```

Answer:

```
 if (n % 2 == 0) {
        printf("n is even\n");
    }
```
The Semi-Colon was removed. 
